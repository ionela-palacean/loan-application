server.port: 8085
spring:
  jpa:
    hibernate.ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/loan_application?serverTimezone=UTC
    username: loan_application
    password: loan_application